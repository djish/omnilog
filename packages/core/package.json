{
  "name": "@omnilog/core",
  "version": "0.1.0",
  "description": "Core logger, registry, and shared types for OmniLog",
  "keywords": ["logging", "logger", "omnilog"],
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "clean": "rimraf dist",
    "lint": "echo \"No linter configured yet\"",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
