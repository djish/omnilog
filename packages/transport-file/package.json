{
  "name": "@omnilog/transport-file",
  "version": "0.1.0",
  "description": "File transport for the OmniLog framework (Node.js only)",
  "keywords": ["logging", "file", "transport", "rotation"],
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "clean": "rimraf dist",
    "lint": "echo \"No linter configured yet\"",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@omnilog/core": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
